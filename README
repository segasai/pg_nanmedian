This module provides a set of  PostgreSQL functions

double precision nanmedian(double precision[])
real nanmedian(real [])
which return the median while ignoring nans

And also functions
double precision nanmad(double precision[])
real nanmad(real [])
which return the median absolute deviations in the arrays
